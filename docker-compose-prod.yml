# =============================================================================
# Stellar Symphony - Docker Compose Production Configuration
# =============================================================================
# Production:  docker-compose up prod
# Build:       docker-compose build
# =============================================================================

services:

  # ---------------------------------------------------------------------------
  # Production Service (static export served via 'serve')
  # ---------------------------------------------------------------------------
  prod:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: stellar-symphony-prod
    expose:
      - "3000"
    environment:
      - NODE_ENV=production
      - VIRTUAL_HOST=stellar.fugas.space,www.stellar.fugas.space
      - VIRTUAL_PORT=3000
      - LETSENCRYPT_HOST=stellar.fugas.space,www.stellar.fugas.space
      - LETSENCRYPT_EMAIL=samaranin2912@gmail.com
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/"]
      interval: 30s
      timeout: 3s
      start_period: 5s
      retries: 3
    networks:
      - lametsy_meteo_net

networks:
  lametsy_meteo_net:
    external: true
